# دليل البدء السريع - Quick Start Guide

## خطوات التشغيل السريعة

### 1. تثبيت المكتبات

#### للمشروع الرئيسي (نظام التصويت):
```bash
npm install
```

#### لمشروع إدارة المستخدمين:
```bash
cd "User Management"
npm install
```

### 2. تشغيل المشاريع

#### تشغيل نظام التصويت:
```bash
npm run dev
```
سيتم فتح المشروع على: `http://localhost:5173`

#### تشغيل إدارة المستخدمين:
```bash
cd "User Management"
npm run dev
```
سيتم فتح المشروع على: `http://localhost:5173`

### 3. بناء المشروع للإنتاج

#### بناء نظام التصويت:
```bash
npm run build
```

#### بناء إدارة المستخدمين:
```bash
cd "User Management"
npm run build
```

## ملاحظات مهمة

- تأكد من تثبيت Node.js (الإصدار 16 أو أحدث)
- استخدم `npm install` في كل مجلد مشروع قبل التشغيل
- المشروعان مستقلان تمامًا ويمكن تشغيلهما بشكل منفصل

## استكشاف الأخطاء

إذا واجهت مشاكل:
1. احذف مجلد `node_modules` وملف `package-lock.json`
2. قم بتشغيل `npm install` مرة أخرى
3. تأكد من أنك في المجلد الصحيح عند تشغيل الأوامر
